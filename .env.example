# Hub uses an embedded database under $XDG_DATA_HOME/paseo-hub by default, falling back to
# ~/.local/share/paseo-hub. Set DATABASE_URL to use PostgreSQL.
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/paseo_hub
# PASEO_HUB_DATA_DIR=/path/to/paseo-hub-data
PASEO_HUB_APP_URL=http://localhost:3000
# Optional advanced override. Hub otherwise generates and stores a stable secret in its database.
# PASEO_HUB_AUTH_SECRET=
# For an existing 90/10 instance, use Google Workspace sign-in instead of passwords.
# The organization slug must already exist; Google OAuth callback:
# https://paseo.9010.berlin/api/auth/callback/google
# PASEO_AUTH_MODE=google
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# PASEO_GOOGLE_ORGANIZATION_SLUG=
# Admission and organization provisioning policy. These defaults keep a new instance closed
# until its owner is bootstrapped and members are invited.
PASEO_REGISTRATION_MODE=invite_only
PASEO_ORGANIZATION_CREATION=disabled
# Set all three bootstrap values together for a fresh or migrated instance.
PASEO_BOOTSTRAP_ORGANIZATION=
PASEO_BOOTSTRAP_OWNER_EMAIL=
PASEO_BOOTSTRAP_OWNER_PASSWORD=
# GitHub connection and webhook authority. The private key may instead use
# GITHUB_APP_PRIVATE_KEY_PATH.
GITHUB_APP_SLUG=
GITHUB_APP_CLIENT_ID=
GITHUB_APP_CLIENT_SECRET=
GITHUB_APP_ID=
GITHUB_APP_PRIVATE_KEY=
GITHUB_WEBHOOK_SECRET=
# Discord connection and authenticated gateway authority.
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=
DISCORD_BOT_TOKEN=
# Optional Slack app authority. Leave this whole group commented to configure Slack in Hub.
# Socket Mode (no public HTTPS):
# SLACK_TRANSPORT=socket
# SLACK_APP_ID=A0123456789
# SLACK_APP_TOKEN=xapp-example
# Webhooks (public HTTPS):
# SLACK_TRANSPORT=webhook
# SLACK_APP_ID=A0123456789
# SLACK_CLIENT_ID=1234567890.1234567890
# SLACK_CLIENT_SECRET=example
# SLACK_SIGNING_SECRET=example
# Optional Linear OAuth and signed webhook authority. Leave this whole group commented to
# configure Linear in Hub. Configure Linear's webhook to send events to
# /api/integrations/linear/events using the same secret below.
# LINEAR_CLIENT_ID=
# LINEAR_CLIENT_SECRET=
# LINEAR_WEBHOOK_SECRET=
# Optional billing integration. Leave these unset to expose no billing routes, navigation, or UI.
# Both are required together: setting STRIPE_SECRET_KEY
# without STRIPE_WEBHOOK_SECRET fails startup, since the webhook signature could not be verified.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
# Optional account and invitation email delivery. Both are required together; the sender must be
# verified for the configured Resend account. Configured instances verify new password accounts
# and support password reset. Leave these unset to keep the copy-link invitation flow.
RESEND_API_KEY=
RESEND_FROM=
# Native Linear agent target (no Hub trigger required). Set all four required values explicitly.
# LINEAR_AGENT_DAEMON=
# LINEAR_AGENT_CWD=
# LINEAR_AGENT_PROVIDER=codex
# LINEAR_AGENT_MODE=auto-review
# LINEAR_AGENT_MODEL=
# Optional: isolate each session in a Git worktree based on this ref.
# LINEAR_AGENT_BASE_BRANCH=origin/main
